import UserHeader from './UserHeader';
import MainHeader from './MainHeader';

const Header = () => {
    return (
        <>
            <UserHeader />
            <MainHeader />
        </>
    );
};

export default Header;
